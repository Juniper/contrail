#!/bin/bash

CONTRAIL_DIR=${1:-$(pwd)}
CONFIG_DIR=${2:-/tmp/contrail_configs}

mkdir -p $CONFIG_DIR

sed -e "s|%CONTRAIL_DIR%|$CONTRAIL_DIR|g" -e "s|%CONFIG_DIR%|$CONFIG_DIR|g" $(dirname "$0")/contrail-ui-with-go-multicloud.yml.tmpl > generated-contrail-ui-with-go-multicloud.yml
