# Binaries for programs and plugins
*.exe
*.dll
*.so
*.dylib

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

.DS_Store
.AppleDouble
.LSOverride

# Project specific paths
dist/
pkg/common/test_output/
profile.cov
tmp/
