database:
    connection: "root:contrail123@tcp(localhost:3306)/contrail_test"
    max_open_conn: 100
log_level: debug
server:
    read_timeout: 10
    write_timeout: 5
address: ":9091"
tls:
    enabled: false
    key_file: key
    cert_file: cret
static_files:
    /: /usr/share/contrail/public
proxy:
    /config:
    - http://localhost:8082
    /telemetry:
    - http://localhost:8081
