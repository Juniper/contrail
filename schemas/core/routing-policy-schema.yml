---
  prefix: "/"
  api_style: "contrail"
  extends: 
    - "base"
  references: 
    routing_instance: 
      operations: "CRUD"
      $ref: "types.json#definitions/RoutingPolicyType"
      description: ""
      presence: "Reference to internal routing instance object automatically generated by system. Reference has property of sequence number which is order of in which routing policies are applied to given routing instance."
    service_instance: 
      operations: "CRUD"
      $ref: "types.json#definitions/RoutingPolicyServiceInstanceType"
      description: "Reference to routing policy attached to (service instance, interface)."
      presence: "optional"
  parents: 
    project: 
      operations: "CRUD"
      description: "List of references of child routing policy objects. automatically maintained by system."
      presence: "optional"
  plural: "routing_policys"
  id: "routing_policy"
  schema: 
    required: []
    type: "object"
    properties: 
      routing_policy_entries: 
        operations: "CRUD"
        presence: "optional"
        description: ""
        $ref: "types.json#/definitions/PolicyStatementType"

