api_style: contrail
extends:
    - base
id: logical_interface
parents:
    physical_interface:
        description: 'Logical interfaces on physical interface on physical routers.'
        operations: CRUD
        presence: optional
    physical_router:
        description: 'Logical interfaces on physical routers.'
        operations: CRUD
        presence: optional
plural: logical_interfaces
prefix: /
references:
    virtual_machine_interface:
        description: 'References to virtual machine interfaces that represent end
            points that are reachable by this logical interface.'
        operations: CRUD
        presence: optional
schema:
    properties:
        logical_interface_type:
            $ref: types.json#/definitions/LogicalInterfaceType
            operations: CRUD
            presence: optional
        logical_interface_vlan_tag:
            description: 'VLAN tag (.1Q) classifier for this logical interface.'
            operations: CRUD
            presence: optional
            type: integer
    required: []
    type: object
