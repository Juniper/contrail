api_style: contrail
extends:
- base
id: access_control_list
parents:
  security_group:
    derived: true
    description: port access control list is  automatically derived from all the security
      groups attached to port.
    operations: CRUD
    presence: system-only
  virtual_network:
    derived: true
    description: Virtual network access control list are automatically derived from
      all the network policies attached to virtual network.
    operations: CRUD
    presence: system-only
plural: access_control_lists
prefix: /
references: {}
schema:
  properties:
    access_control_list_entries:
      $ref: types.json#/definitions/AclEntriesType
      description: Automatically generated by system based on security groups or network
        policies.
      operations: CRUD
      presence: system-only
    access_control_list_hash:
      description: A hash value of all the access-control-list-entries in this ACL
        objects automatically generated by system.
      operations: CRUD
      presence: system-only
      type: integer
  required: []
  type: object
