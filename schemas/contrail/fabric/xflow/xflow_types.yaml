definitions:
  ClickhouseType:
    type: object
    properties:
      retention_period_secs:
        type: integer
        presence: optional
      retention_size_bytes:
        type: integer
        presence: optional
