definitions:
  XflowType:
    type: object
    properties:
      iface_public:
        presence: optional
        type: string
      iface_internal:
        presence: optional
        type: string
  ClickhouseType:
    type: object
    properties:
      retention_period_secs:
        type: integer
        presence: optional
      retention_size_bytes:
        type: integer
        presence: optional
