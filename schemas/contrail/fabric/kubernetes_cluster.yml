description: Kubernetes Cluster
extends:
- base
- ownable
- has_status
id: kubernetes_cluster
metadata:
  category: cluster
parents:
  global_system_config:
    operations: "CRUD"
    description: "Parent of this node resource."
    presence: "optional"
plural: kubernetes_clusters
prefix: /
schema:
  properties:
    ntp_server:
      description: NTP server ip address
      nullable: false
      permission:
      - create
      - update
      sql: ""
      title: NTPServer
      type: string
      view:
      - create
      - detail
    kubernetes_public_fip_pool:
      description: Kubernetes Public FIP Pool.
      nullable: false
      permission:
      - create
      - update
      sql: text
      title: Kubernetes Public FIP Pool.
      type: string
      view:
      - detail
singular: kubernetes_cluster
title: Kubernetes Cluster
type: ""
